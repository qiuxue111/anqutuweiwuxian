# 2026-06-02

## 贡献榜实现 (index.html)
- 追加 `loadLeaderboard()` 函数，从 `pins` 表取所有 `submitted_by` 字段，统计每个用户点位数量，排序显示前15名
- 显示🥇🥈🥉奖牌 + 进度条 + 数量
- 下方追加领取说明："每100点位可找秋雪领取15块..." 带入场动画 + QQ链接跳转
- 说明文字块入场动画用内联 `animation:enterPop`，延迟 0.3s

## submitted_by 数据清洗
- 审核审批 API 直接调用：`POST /rest/v1/pins` + `DELETE from pending_pins`
- `POST` body 不能包含 `approved_by` 列（pins 表无此列）
- 通过 API 把 `pins` 表中 34 条 `submitted_by='匿名'` 改为 `qiuxue111`
- 修复 5 条 JWT token JSON 格式的 `submitted_by` → 提取 `preferred_username`

## 审核页面 (review.html) 定位地图弹窗优化
- 初始缩放 `zm=3` → `zm=1300`
- 缩放上限 `20` → `1600`
- 标注点尺寸动态计算：
  - 红点：`Math.min(18, 2000/zm)`
  - 图标：`Math.min(48, 5000/zm)`
  - 边框：`Math.min(3, 300/zm)`
  - 文字：`Math.min(14, 1500/zm)`
- `updatePinSize()` 函数定义在 `label` 之后调用（修复 undefined 错误）
- 所有缩放操作（按钮/滚轮/重置/楼层切换）都调用 `updatePinSize()`

## 审核审批操作
- 27 条 pending_pins 全部通过并写入 pins 表（农场点位）
- 批量通过的 Node.js 脚本逻辑：POST 到 pins → DELETE from pending_pins

## Git 提交记录
- `f5fb126` feat: 主页点位贡献榜
- `bc03063` feat: 贡献榜下方加领取说明
- `45c9028` feat: 贡献榜说明加入场动画+QQ链接
- `0b5236a` fix: 贡献榜说明入场动画改用内联keyframes
- `7ca7774` fix: 审核页定位地图放大3→1300倍
- `59891fa` fix: 审核页地图弹窗 - zm=1300,动态标注点尺寸,缩放上限1600
- `e371b6c` fix: updatePinSize在label定义后调用
